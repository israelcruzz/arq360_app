import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  primary: {
    fontSize: 16,
    fontWeight: 'regular',
    color: '#2A2A2A'
  },
  secondary: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#151515'
  }
});
