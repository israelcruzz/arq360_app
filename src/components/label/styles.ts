import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  labelArea: {
    marginBottom: 12,
    marginTop: 12
  },
  labelText: {
    fontSize: 16,
    color: '#2A2A2A',
    fontWeight: 'regular'
  }
});
